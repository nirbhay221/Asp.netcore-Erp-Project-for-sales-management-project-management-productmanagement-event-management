
<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>ClosedWon Project List</title>
    <link href="~/Content/bootstrap.min.css" rel="stylesheet" />
    <script type="text/javascript" src="@Url.Action("~/scripts/jquery-3.3.1.min.js")"></script>
    <script type="text/javascript" src="@Url.Action("~/scripts/bootstrap.min.js")"></script>
</head>
<body>
    <a asp-controller="ClosedWon" asp-action="add">Add</a>
    <div></div><div></div>
    <a asp-controller="mainhome" asp-action="newindex">Index</a>
    <div class="container-fluid py-4">
        <h4 class="text-center text-uppercase">ClosedWon Sales List</h4>
        <table class="table table-striped table-bordered">
            <thead class="bg-dark text-white">
                <tr>

                    <th>Id</th>
                    <th>Name</th>
                    <th>Rep</th>
                    <th>SalesStage</th>
                    <th>Priority</th>
                    <th>ExpectedCloseDate</th>
                    <th>ForeCastValue</th>
                    <th>Probability</th>
                    <th>LastContact</th>
                    <th>SignedContractValue</th>
                    <th>Notes</th>

                    <th>Options</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var ClosedWon in ViewBag.ClosedWon)
                {
                    <tr>
                        <td>@ClosedWon.Id</td>
                        <td>@ClosedWon.Name</td>
                        <td>@ClosedWon.Rep</td>
                        <td>@ClosedWon.SalesStage</td>

                        <td>@ClosedWon.Priority</td>

                        <td>@ClosedWon.ExpectedCloseDate</td>
                        <td>@ClosedWon.ForeCastValue</td>
                        <td>@ClosedWon.Probability</td>
                        <td>@ClosedWon.LastContact</td>
                        <td>@ClosedWon.SignedContractValue</td>
                        <td>@ClosedWon.Notes</td>

                        <td>
                            <a asp-controller="ClosedWon" asp-action="edit" asp-route-id="@ClosedWon.Id">Edit</a>
                            |
                            <a asp-controller="ClosedWon" asp-action="delete" asp-route-id="@ClosedWon.Id" onclick="return confirm('Are you sure ?')">Delete</a>
                        </td>


                    </tr>

                }
            </tbody>
        </table>
    </div>
</body>
</html>
<style>
    body {
        background: url('https://images.pexels.com/photos/1323712/pexels-photo-1323712.jpeg?auto=compress&cs=tinysrgb&dpr=1&w=500') no-repeat fixed center center;
        background-size: cover;
        font-family: Montserrat;
    }
</style>