
<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>AtRisk Project List</title>
    <link href="~/Content/bootstrap.min.css" rel="stylesheet" />
    <script type="text/javascript" src="@Url.Action("~/scripts/jquery-3.3.1.min.js")"></script>
    <script type="text/javascript" src="@Url.Action("~/scripts/bootstrap.min.js")"></script>
</head>
<body>
    <a asp-controller="Blocked" asp-action="add">Add Project</a>
    <div></div><div></div>
    <a asp-controller="mainhome" asp-action="newindex">Index</a>
    <div class="container-fluid py-4">
        <h4 class="text-center text-uppercase">Blocked Project List</h4>
        <table class="table table-striped table-bordered">
            <thead class="bg-dark text-white">
                <tr>
                    <th>Id</th>
                    <th>Name</th>
                    <th>Status</th>
                    <th>Category</th>
                    <th>Owner</th>
                    <th>Lead</th>
                    <th>Budget</th>
                    <th>Profit</th>
                    <th>Options</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var Blocked in ViewBag.Blocked)
                {
                    <tr>
                        <td>@Blocked.Id</td>
                        <td>@Blocked.Name</td>
                        <td>@Blocked.Status</td>
                        <td>@Blocked.Category</td>

                        <td>@Blocked.Owner</td>

                        <td>@Blocked.Lead</td>

                        <td>@Blocked.Budget</td>
                        <td>@Blocked.Profit</td>

                        <td>
                            <a asp-controller="Blocked" asp-action="edit" asp-route-id="@Blocked.Id">Edit</a>
                            |
                            <a asp-controller="Blocked" asp-action="delete" asp-route-id="@Blocked.Id" onclick="return confirm('Are you sure ?')">Delete</a>
                        </td>


                    </tr>

                }
            </tbody>
        </table>
    </div>
</body>
</html>
<style>
    body {
        background: url('https://images.pexels.com/photos/1323712/pexels-photo-1323712.jpeg?auto=compress&cs=tinysrgb&dpr=1&w=500') no-repeat fixed center center;
        background-size: cover;
        font-family: Montserrat;
    }
</style>